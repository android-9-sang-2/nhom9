<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chanh.toeic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">

            <!-- android:label="TOEIC Test" -->
            <!-- android:parentActivityName=".MainActivity" -->
            <!-- android:theme="@style/AppTheme.NoActionBar"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
         <activity android:name=".TestListActivity" />
         <activity
         android:name=".TestPageActivity"
         android:label="@string/title_activity_test_page"
         android:theme="@style/AppTheme.NoActionBar" />
         <activity android:name=".ResultActivity" />
         <activity android:name=".SimulationTestActivity" />




        <!-- <activity android:name=".Activity.Main2Activity"></activity> -->
         <activity android:name=".Activity.ScreenSlideActivity"></activity>
        <activity android:name=".Activity.chamDiem_P5Activity" />
        <!--<activity android:name=".Activity.Main2Activity"></activity>-->
    </application>

</manifest>